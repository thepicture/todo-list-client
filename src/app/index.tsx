import { Card, Typography } from '@mui/material';
import React from 'react';

import './index.scss';

export const App = () => {
	return (
		<Card>
			<Typography>Initial</Typography>
		</Card>
	);
};
